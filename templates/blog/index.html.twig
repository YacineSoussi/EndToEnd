{% extends 'base.html.twig' %}

{% block title %}Blog index{% endblock %}

{# {% block body %}
    

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Content</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for blog in blogs %}
            <tr>
                <td>{{ blog.id }}</td>
                <td>{{ blog.title }}</td>
                <td>{{ blog.content }}</td>
                <td>
                    <a href="{{ path('app_blog_show', {'id': blog.id}) }}">show</a>
                    <a href="{{ path('app_blog_edit', {'id': blog.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_blog_new') }}">Create new</a>
{% endblock %} #}


    

{% block body %}
<div class="container">
<h1>Blog index</h1>
{% for blog in blogs %}
    <div class="card" style="width: 18rem; padding: 2%;">
  <img src="https://dummyimage.com/400x200/563d7c/ffffff&text=.card-img-top" class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title">{{blog.title}}</h5>
    <p class="card-text">{{blog.content}}</p>
    <a class="btn btn-success" href="{{ path('app_blog_show', {'id': blog.id}) }}">show</a>
    <a class="btn btn-primary" href="{{ path('app_blog_edit', {'id': blog.id}) }}">edit</a>
  </div>
</div>
{% endfor %}
<a href="{{ path('app_blog_new') }}" class="btn btn-primary">Create new</a>
</div>
{% endblock %}